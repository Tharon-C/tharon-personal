import styled from 'styled-components'
import css from '@styled-system/css'

const Main = styled.div(css({
      margin: 'auto',
      maxWidth: 980,
      py: [2, 2, 4],
      px: [2, 2, 3]
    }))

export default Main
